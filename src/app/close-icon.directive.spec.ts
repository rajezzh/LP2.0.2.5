import { CloseIconDirective } from './close-icon.directive';

describe('CloseIconDirective', () => {
  it('should create an instance', () => {
    const directive = new CloseIconDirective();
    expect(directive).toBeTruthy();
  });
});
